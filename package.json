{
  "name": "namecoin-explorer",
  "version": "2.0.0",
  "description": "Industrial-grade, Namecoin-native name browser & operations explorer. Browse the full on-chain registry, trace complete provenance, follow live name operations, and query a JSON API — backed by your own full node.",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "check": "node --check app.js && for f in lib/*.js; do node --check \"$f\"; done",
    "test": "echo \"No test runner configured yet — see CONTRIBUTING.md\" && exit 0"
  },
  "keywords": [
    "namecoin",
    "nmc",
    "blockchain",
    "explorer",
    "dns",
    "names",
    "block-explorer",
    "open-source"
  ],
  "author": "historicsxyz",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/historicsxyz/namecoin-explorer.git"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^9.4.3",
    "bulma": "^1.0.4",
    "compression": "^1.7.4",
    "ejs": "^3.1.9",
    "express": "^4.18.2",
    "express-ejs-layouts": "^2.5.1",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}